---
import { Icon } from "astro-icon/components";
interface Props {
  class?: string;
  [key: string]: any;
}
const { class: className, ...rest } = Astro.props;
---

<Icon name="lucide:copy" class={className} aria-label="Copy" {...rest} />
