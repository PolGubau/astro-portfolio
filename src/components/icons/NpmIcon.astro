---
import { Icon } from "astro-icon/components";
interface Props {
  class?: string;
  [key: string]: any;
}
const { class: className, ...rest } = Astro.props;
---

<Icon name="simple-icons:npm" class={className} aria-label="npm" {...rest} />
