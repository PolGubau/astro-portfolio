---
import { Icon } from "astro-icon/components";
interface Props {
  class?: string;
  [key: string]: unknown;
}
const { class: className, ...rest } = Astro.props;
---

<Icon
  name="simple-icons:googleplay"
  class={className}
  aria-label="Google Play"
  {...rest}
/>
