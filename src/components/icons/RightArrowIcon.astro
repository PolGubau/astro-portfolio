---
import { Icon } from "astro-icon/components";
interface Props {
  class?: string;
  [key: string]: any;
}
const { class: className, ...rest } = Astro.props;
---

<Icon
  name="lucide:arrow-right"
  class={className}
  aria-hidden="true"
  {...rest}
/>
