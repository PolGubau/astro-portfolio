---
// English blog index (/blog). Localized variants live at /[locale]/blog; the
// shared UI chrome, meta and schema live in BlogPage.
import BlogPage from "../../components/pages/BlogPage.astro";
---

<BlogPage />
