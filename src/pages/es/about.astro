---
import { Image } from "astro:assets";
import teaching from "../../assets/media/about/teaching.avif";
import tfg from "../../assets/media/about/tfg.avif";
import tv3 from "../../assets/media/about/tv3.avif";
import me from "../../assets/media/me.webp";
import { getLocale, useTranslations } from "../../i18n/utils";
import MainLayout from "../../layouts/MainLayout.astro";
import { metadata } from "../../lib/constants";

const locale = getLocale(Astro.currentLocale);
const t = useTranslations(locale);

const profilePageSchema = {
  "@context": "https://schema.org",
  "@type": "ProfilePage",
  name: `About ${metadata.longName}`,
  url: `${metadata.links.website}/es/about`,
  description:
    "Conoce a Pol Gubau Amores, Senior Frontend Engineer en Mataró, Barcelona. Cofundador de Doscientos, creador de Pol UI.",
  mainEntity: {
    "@type": "Person",
    "@id": `${metadata.links.website}/#person`,
    name: metadata.longName,
    alternateName: ["Pol Gubau", "PolGubau"],
    jobTitle: metadata.slogan,
    url: metadata.links.website,
    image: `${metadata.links.website}/thumbnail.webp`,
    email: "hola@polgubau.com",
    address: {
      "@type": "PostalAddress",
      addressLocality: "Mataró",
      addressRegion: "Barcelona",
      addressCountry: "ES",
    },
    sameAs: [
      metadata.links.github,
      metadata.links.linkedin,
      metadata.links.twitter,
      metadata.links.npm,
    ],
  },
};
---

<MainLayout
  title="Sobre Pol Gubau Amores - Senior Frontend Engineer & Diseñador"
  description="Conoce a Pol Gubau Amores, Senior Frontend Engineer en Mataró, Barcelona. Cofundador de Doscientos, creador de Pol UI, diseñador reconvertido en desarrollador."
>
  <script
    is:inline
    type="application/ld+json"
    set:html={JSON.stringify(profilePageSchema)}
  />
  <article class="flex flex-col gap-14 py-8 max-w-2xl">
    <!-- Hero -->
    <header class="flex flex-col gap-6" data-reveal="bottom" data-speed="fast">
      <div class="flex items-center gap-5">
        <Image
          src={me}
          alt="Pol Gubau Amores"
          format="avif"
          width={72}
          height={72}
          loading="eager"
          class="rounded-full object-cover bg-primary shrink-0"
        />
        <div>
          <h1 class="text-2xl font-semibold tracking-tight">
            Pol Gubau Amores
          </h1>
          <p class="text-ink/50">{t("about.role")}</p>
        </div>
      </div>
      <p
        class="text-lg leading-relaxed text-ink/80"
        set:html={t("about.hero")}
      />
    </header>

    <!-- Story -->
    <section
      class="flex flex-col gap-4 text-ink/75 leading-relaxed"
      data-reveal="bottom"
    >
      <h2 class="text-xl font-semibold text-ink">{t("about.story.title")}</h2>
      <p set:html={t("about.story.p1")} />
      <p set:html={t("about.story.p2")} />
      <p set:html={t("about.story.p3")} />
    </section>

    <!-- Stats -->
    <div
      data-reveal="bottom"
      class="grid grid-cols-2 md:grid-cols-4 gap-px bg-ink/80 rounded-xl overflow-hidden border border-ink/50"
    >
      {
        [
          {
            value: `${new Date().getFullYear() - 2019}+`,
            label: t("about.stats.years"),
          },
          { value: "62k+", label: t("about.stats.npm") },
          { value: "150+", label: t("about.stats.components") },
          { value: "50+", label: t("about.stats.projects") },
        ].map(({ value, label }) => (
          <div class="flex flex-col gap-1 px-6 py-5 bg-background">
            <span class="text-2xl font-medium">{value}</span>
            <span class="text-sm text-ink/50">{label}</span>
          </div>
        ))
      }
    </div>

    <!-- Photos -->
    <section class="flex flex-col gap-6" data-stagger="100">
      <h2 class="text-xl font-semibold text-ink" data-reveal="bottom">
        {t("about.beyond.title")}
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <figure class="flex flex-col gap-2" data-reveal="bottom">
          <Image
            src={tv3}
            alt="Pol Gubau interviewed on TV3"
            format="avif"
            width={400}
            height={300}
            class="w-full aspect-[4/3] object-cover rounded-xl border border-ink/50"
          />
          <figcaption class="text-sm text-ink/50">
            {t("about.beyond.caption1")}
          </figcaption>
        </figure>
        <figure class="flex flex-col gap-2" data-reveal="bottom">
          <Image
            src={teaching}
            alt="Pol Gubau teaching students"
            format="avif"
            width={400}
            height={300}
            class="w-full aspect-[4/3] object-cover rounded-xl border border-ink/50"
          />
          <figcaption class="text-sm text-ink/50">
            {t("about.beyond.caption2")}
          </figcaption>
        </figure>
        <figure class="flex flex-col gap-2" data-reveal="bottom">
          <Image
            src={tfg}
            alt="Pol Gubau presenting his thesis"
            format="avif"
            width={400}
            height={300}
            class="w-full aspect-[4/3] object-cover rounded-xl border border-ink/50"
          />
          <figcaption class="text-sm text-ink/50">
            {t("about.beyond.caption3")}
          </figcaption>
        </figure>
      </div>
    </section>

    <!-- Currently -->
    <section class="flex flex-col gap-4" data-reveal="bottom">
      <h2 class="text-xl font-semibold text-ink">
        {t("about.currently.title")}
      </h2>
      <ul class="flex flex-col gap-3 text-ink/70">
        {
          [
            t("about.currently.item1"),
            t("about.currently.item2"),
            t("about.currently.item3"),
            t("about.currently.item4"),
          ].map((item) => (
            <li class="flex gap-3">
              <span class="text-ink/30 shrink-0">→</span>
              <span set:html={item} />
            </li>
          ))
        }
      </ul>
    </section>

    <!-- Contact -->
    <section
      class="flex flex-col gap-4 pt-6 border-t border-ink/10"
      data-reveal="bottom"
    >
      <h2 class="text-xl font-semibold text-ink">{t("about.contact.title")}</h2>
      <p class="text-ink/60">{t("about.contact.body")}</p>
      <div class="flex flex-wrap gap-3">
        <a
          href={metadata.links.email}
          class="inline-flex items-center gap-2 rounded-xl border border-ink/15 bg-ink text-background px-4 py-2.5 text-sm font-medium transition-[transform,box-shadow] duration-200 ease-[cubic-bezier(0.25,1,0.5,1)] hover:-translate-y-0.5 hover:shadow-md hover:bg-ink/85"
          >{t("about.contact.email")}</a
        >
        <a
          href={metadata.links.linkedin}
          target="_blank"
          rel="me noopener noreferrer"
          class="inline-flex items-center gap-2 rounded-xl border border-ink/10 bg-background text-ink px-4 py-2.5 text-sm font-medium transition-[transform,box-shadow,border-color] duration-200 ease-[cubic-bezier(0.25,1,0.5,1)] hover:-translate-y-0.5 hover:shadow-md hover:border-ink/25"
          >LinkedIn</a
        >
        <a
          href={metadata.links.github}
          target="_blank"
          rel="me noopener noreferrer"
          class="inline-flex items-center gap-2 rounded-xl border border-ink/10 bg-background text-ink px-4 py-2.5 text-sm font-medium transition-[transform,box-shadow,border-color] duration-200 ease-[cubic-bezier(0.25,1,0.5,1)] hover:-translate-y-0.5 hover:shadow-md hover:border-ink/25"
          >GitHub</a
        >
      </div>
    </section>
  </article>
</MainLayout>
