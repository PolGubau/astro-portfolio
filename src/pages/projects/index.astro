---
// English projects index (/projects). Localized variants live at
// /[locale]/projects; the shared UI chrome, meta and schema live in
// ProjectsPage.
import ProjectsPage from "../../components/pages/ProjectsPage.astro";
---

<ProjectsPage />
